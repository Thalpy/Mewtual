{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs (events for the actor's ChannelUpdated/MembersChanged, window). Custom app commands (found_server, send_message, get_messages) are callable without an explicit ACL entry.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
